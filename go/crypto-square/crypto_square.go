package cryptosquare

import "strings"

func Encode(pt string) string {
	return strings.Join(transpose(rectangle(pt)), " ")
}

func transpose(rows [][]byte) []string {
	out := []string{}


	
	return out
}

func rectangle(input string) [][]byte {
	out := [][]byte{}

	return out
}

func smallestRectangle(length int) []int {
	for col, row := length, 1; col < count; col++ {
		
	}
	r * c >= length of message,
	and c >= r,
	and c - r <= 1.
}
